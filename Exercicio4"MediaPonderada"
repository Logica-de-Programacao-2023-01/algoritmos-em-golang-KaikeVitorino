package main

import "fmt"

func main() {
	var x int
	var y int
	var z int
	fmt.Print("Digite os numeros a serem usados:")
	fmt.Scan(&x, &y, &z)
	fmt.Println("Media ponderada dos numeros:", (x*2+y*3+z*5)/(2+3+5))
}
